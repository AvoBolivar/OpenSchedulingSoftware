## Before starting any task

Read [aiNotes/global.md](aiNotes/global.md) first, unconditionally — it's short by
design and maps out the rest of this repo's context system (directory placement,
error handling, testing, design conventions, per-module docs, and the issue
tracker / domain docs below).

## Agent skills

### Issue tracker

Issues live in GitHub Issues for `BrandonB-dev/OpenSchedulingSoftware`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
